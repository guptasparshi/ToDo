const users = []
const tokens=[]
const tasks=[]

const adminTokens=[]
const admin=[]


module.exports={
    users,
    tokens,
    tasks,
    adminTokens,
    admin
}

//if anything doesn't work, make a fun(newArray) for each array and override the existing array with the new array